package meminterface {
    import conf::*;
    struct Req {
        ready: logic       ,
        valid: logic       ,
        addr : conf::Addr  ,
        wen  : logic       ,
        wdata: conf::UInt64,
        // addr : Addr , // TODO 名前空間がおかしそうなエラー
        // wdata: UInt64, // TODO 名前空間がおかしそうなエラー
    }
    struct Resp {
        valid: logic       ,
        rdata: conf::UInt64,
    }
}

module top_pynq (
    CLK   : input  logic   ,
    RESETN: input  logic   ,
    LED   : output logic<4>,
) {
    var led_top: logic<6>;
    assign LED     = led_top[3:0];

    inst t: top (
        clk: CLK    ,
        rst: RESETN ,
        led: led_top,
    );
}
